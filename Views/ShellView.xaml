<Window x:Class="TimeBox.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeBox"
        mc:Ignorable="d"
        Background="{Binding ChromaKeyColor}"
        Title="{Binding AppTitle}" Height="270" Width="910" MinWidth="360" MinHeight="255">

    <Grid Margin="12">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="140" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <ContentControl x:Name="ClockView" Grid.Column="0" Grid.Row="0" Content=""></ContentControl>
        <ContentControl x:Name="QuotesView" Grid.Column="1" Grid.Row="0" Grid.RowSpan="3" Content=""></ContentControl>

        <Label x:Name="DisplayTitle" Grid.Column="0" Grid.Row="1" FontSize="32" FontFamily="Bahnschrift SemiCondensed" Background="#0B2647" Foreground="LightGray" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Margin="8" />
        
    </Grid>

</Window>
